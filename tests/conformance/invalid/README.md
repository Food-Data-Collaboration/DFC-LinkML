# invalid/ — placeholder for the validation phase

No invalid-document assertions yet. Planned: SHACL-backed `validate()`
with `valid: bool` + structured errors per `docs/sdk-contract.md` (future),
plus fixtures for missing required properties, bad datatypes, unknown
terms, and broken references.
